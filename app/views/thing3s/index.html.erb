<h1>Thing3s index</h1>
<table>
<% @things.each do |thing| %>
  <tr>
    <td>Name: <%= link_to thing.thing_name, thing3_path(thing) %></td>
    <td><%= link_to "Destroy", thing3_path(thing), :method => :delete %></td>
  </tr>
<% end %>
</table>
